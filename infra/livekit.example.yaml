# Copy to livekit.yaml next to docker-compose.prod.yml on the server.
# Keys must match LIVEKIT_API_KEY/SECRET in .env.prod.
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true
keys:
  prodkey: change-me-livekit-secret
