name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prisma generate
        working-directory: packages/db
        env:
          DATABASE_URL: postgresql://user:password@localhost:5432/db
      - run: pnpm run lint
      - run: pnpm run check-types
      - uses: oven-sh/setup-bun@v2 # validate script runs TS directly via bun (repo convention)
      - run: pnpm --filter @verse/assets run validate # §27 provenance gate
      # ponytail: add vitest + playwright jobs when first app with tests lands (plan appendix step 6)
