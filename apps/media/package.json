{
  "name": "@repo/media",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "check-types": "tsc --noEmit",
    "test:spike": "bun run test/token.spike.ts",
    "test:auth": "bun run test/auth.spike.ts"
  },
  "dependencies": {
    "@repo/auth": "workspace:*",
    "livekit-server-sdk": "^2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  }
}
