# Anthropic API key — without it /v1/ai/notes returns 503
ANTHROPIC_API_KEY=sk-ant-...
# must match the gateway's JWT_SECRET — ai verifies gateway-issued access tokens
JWT_SECRET=dev-gateway-secret-do-not-use-in-prod
# AI_PORT=2570
